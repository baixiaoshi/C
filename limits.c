#include <stdio.h>
#include <limits.h>

int main(void) {
    printf("the number of bits in a byte %d\n", CHAR_BIT);
    printf("int min=%d\n", INT_MIN);
    printf("int max=%d\n", INT_MAX);
    return 0;
}
